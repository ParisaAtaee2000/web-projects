"use client";
import Link from "next/link";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";
import { useCart } from "@/components/cart/CartProvider";
const money=(value:number)=>`${value.toLocaleString("fa-IR")} تومان`;
export default function CartPage(){const {items,itemCount,subtotal,hasInvalidItems,setQuantity,removeItem}=useCart();return <><Header/><main className="container section"><div className="section-head"><div><span className="section-kicker">WHOLESALE ORDER</span><h1 className="section-title">سفارش عمده</h1><p className="muted">{itemCount.toLocaleString("fa-IR")} عدد در سبد سفارش</p></div><Link className="btn" href="/shop">ادامه انتخاب محصولات</Link></div>{items.length===0?<section className="empty-state"><span className="section-kicker">YOUR ORDER IS EMPTY</span><h2 className="serif">سبد سفارش شما خالی است.</h2><p className="muted">برای شروع، از کالکشن زمستانه محصولات را انتخاب کنید.</p><Link className="btn primary" href="/shop">مشاهده محصولات</Link></section>:<div className="cart-layout"><section className="cart-list">{items.map(item=><article className="cart-item" key={`${item.product.id}-${item.color}-${item.size}`}><div className="cart-art"><span className="cap">{item.product.code}</span></div><div className="cart-content"><div className="cart-copy"><span className="section-kicker">{item.product.category.toUpperCase()}</span><h2>{item.product.name}</h2><p className="muted">رنگ: {item.color} · سایز: {item.size}</p><p className="muted">قیمت واحد عمده: {money(item.product.wholesalePrice)}</p></div><div className="cart-controls"><div className="qty-box"><span>تعداد</span><div className="qty-controls"><button type="button" onClick={()=>setQuantity(item.product.id,item.quantity+1)}>+</button><strong>{item.quantity.toLocaleString("fa-IR")}</strong><button type="button" onClick={()=>setQuantity(item.product.id,item.quantity-1)}>-</button></div></div>{item.quantity<8&&<span className="cart-warning">حداقل سفارش هر محصول ۸ عدد است.</span>}<button className="text-button" type="button" onClick={()=>removeItem(item.product.id)}>حذف</button></div></div></article>)}</section><aside className="order-summary"><span className="section-kicker">ORDER SUMMARY</span><h2 className="serif">خلاصه سفارش</h2><div className="summary-row"><span>تعداد کل</span><strong>{itemCount.toLocaleString("fa-IR")} عدد</strong></div><div className="summary-row"><span>مبلغ کالا</span><strong>{money(subtotal)}</strong></div><div className="summary-row"><span>ارسال</span><strong>محاسبه در ادامه</strong></div>{hasInvalidItems&&<p className="cart-warning">برای ادامه باید حداقل ۸ عدد از هر محصول سفارش دهید.</p>}<Link className={`btn primary product-cta ${hasInvalidItems?"disabled-link":""}`} aria-disabled={hasInvalidItems} href={hasInvalidItems?"/cart":"/checkout"}>ادامه به اطلاعات سفارش</Link><small className="muted">خرید تکی در نسخه فعلی فعال نیست.</small></aside></div>}</main><Footer/></>;
}
