import type { Product } from "@/types/product-model";
import { QuantitySelector } from "./QuantitySelector";

export function ProductInfo({product}:{product:Product}){return <div className="product-info"><div className="kicker mono">PRODUCT / {product.slug}</div><h1>{product.name}</h1><p className="product-copy">فرم شهری و کاربردی برای کالکشن زمستانه فروشگاه‌های مردانه؛ طراحی شده برای خرید عمده با اطلاعات سفارش شفاف.</p><div className="price-box"><div className="wholesale-price">{product.price.toLocaleString("fa-IR")} تومان</div><small>قیمت عمده / هر عدد · قیمت‌گذاری بر اساس سفارش</small></div><div className="variant-group"><div className="variant-label">رنگ</div><div className="swatches">{product.colors.map(c=><span className="swatch" key={c.name} style={{background:c.value}} aria-label={c.name}/>)}</div></div><div className="variant-group"><div className="variant-label">سایز</div><div className="sizes">{product.sizes.map((size,i)=><span className={`size ${i===0?"active":""}`} key={size}>{size}</span>)}</div></div><QuantitySelector moq={product.moq}/><button className="btn primary product-cta">افزودن به سفارش عمده</button><a className="btn product-cta" href="#details">راهنمای سایز و مشخصات</a></div>}
